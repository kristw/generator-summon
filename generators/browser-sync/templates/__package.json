{
  "devDependencies": {
    "browser-sync": "^2.14.0"
  },
  "scripts": {
    "browser-sync": "browser-sync start --server 'dist' --browser \"Google Chrome\" --files \"dist/**/*.*\" --port 7000"
  }
}