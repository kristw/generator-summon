{
  "devDependencies": {
    "np": "^2.16.0",
    "pkgfiles": "^2.3.0"
  },
  "scripts": {
    "preversion": "npm test",
    "version": "npm run build && git add -A dist",
    "prepublish": "pkgfiles"
  }
}